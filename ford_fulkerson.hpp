#include "graph.hpp"

#ifndef FORD_FULKERSON
#define FORD_FULKERSON

double ford_fulkerson_max_flow(const graph_t& graph, idx_t source, idx_t sink){
    double max_flow = 0;
    graph_t residual_graph = graph;

    return max_flow;
}

#endif
